import React from "react";
import { Link } from "react-router-dom";
import { Header } from "../../components/layout/Header";

export const Home = () => {
    return (
        <main>
            <Header />
        </main>
    );
};
